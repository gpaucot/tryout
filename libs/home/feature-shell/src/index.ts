/**
 * home/feature-shell — public API.
 * A feature lib exposes routes, not components: the shell app lazy-loads
 * HOME_ROUTES and never reaches into the pages directly.
 */
export { HOME_ROUTES } from './lib/home.routes';
